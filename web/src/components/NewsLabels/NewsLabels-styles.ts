import { styled } from '@mui/material/styles';

export const StyledDiv = styled("div")({
  width: '100%',
  marginLeft: '16px',
  marginRight: '16px',
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'space-between',
  alignItems: 'center',
});
